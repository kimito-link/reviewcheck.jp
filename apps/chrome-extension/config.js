// 拡張の設定。Web版のドメインに合わせる。
// ローカル開発でWeb版(localhost)を診断したい場合は API_BASE を変更し、
// manifest.json の host_permissions に "http://localhost:3000/*" を追加してください。
export const CONFIG = {
  API_BASE: "https://reviewcheck.jp",
};
